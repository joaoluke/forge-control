# 🌍 Sistema de Internacionalização (i18n)

## Visão Geral

O Forge Control agora suporta múltiplos idiomas! Atualmente disponível em:
- 🇧🇷 **Português** (padrão)
- 🇺🇸 **English**

## Como Funciona

### Tecnologia
- **i18next**: Biblioteca principal de internacionalização
- **react-i18next**: Integração com React
- **i18next-browser-languagedetector**: Detecta idioma do navegador automaticamente

### Estrutura de Arquivos

```
src/
├── i18n.ts                    # Configuração do i18next
├── locales/
│   ├── pt.json               # Traduções em português
│   └── en.json               # Traduções em inglês
└── components/
    └── LanguageSelector.tsx  # Componente seletor de idioma
```

## Como Usar

### No Código

```tsx
import { useTranslation } from 'react-i18next';

function MeuComponente() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('app.title')}</h1>
      <p>{t('app.subtitle')}</p>
    </div>
  );
}
```

### Adicionar Novas Traduções

1. Edite `src/locales/pt.json`:
```json
{
  "minhaChave": {
    "titulo": "Meu Título",
    "descricao": "Minha descrição"
  }
}
```

2. Edite `src/locales/en.json`:
```json
{
  "minhaChave": {
    "titulo": "My Title",
    "descricao": "My description"
  }
}
```

3. Use no componente:
```tsx
<h1>{t('minhaChave.titulo')}</h1>
```

## Funcionalidades

### Detecção Automática
O sistema detecta automaticamente o idioma do navegador do usuário. Se não for PT ou EN, usa PT como padrão.

### Persistência
A escolha do idioma é salva no `localStorage` e mantida entre sessões.

### Seletor de Idioma
O componente `LanguageSelector` permite trocar entre idiomas com um clique:
- Botões estilizados com bandeiras
- Destaque visual do idioma ativo
- Transição suave entre idiomas

## Adicionar Novos Idiomas

Para adicionar um novo idioma (ex: Espanhol):

1. Crie `src/locales/es.json` com as traduções
2. Atualize `src/i18n.ts`:
```ts
import es from './locales/es.json';

i18n.init({
  resources: {
    en: { translation: en },
    pt: { translation: pt },
    es: { translation: es }, // Novo idioma
  },
  supportedLngs: ['en', 'pt', 'es'],
  // ...
});
```

3. Adicione botão no `LanguageSelector.tsx`:
```tsx
<button onClick={() => changeLanguage('es')}>
  🇪🇸 Español
</button>
```

## Traduções Disponíveis

### Estrutura Atual

```json
{
  "app": {
    "title": "Título do app",
    "subtitle": "Subtítulo"
  },
  "test": {
    "title": "Título do teste",
    "description": "Descrição",
    "button": "Texto do botão",
    "loading": "Carregando...",
    "error": "Erro",
    "success": "Sucesso"
  },
  "system": {
    "os": "Sistema Operacional",
    "version": "Versão",
    "hostname": "Hostname"
  },
  "footer": {
    "phase": "Fase atual",
    "stack": "Stack tecnológica"
  },
  "language": {
    "label": "Idioma",
    "portuguese": "Português",
    "english": "English"
  }
}
```

## Próximos Passos

Quando implementar novas features (FASE 1, 2, etc.), lembre-se de:
1. Adicionar traduções em ambos os arquivos (pt.json e en.json)
2. Usar `t('chave')` ao invés de texto hardcoded
3. Manter a estrutura organizada por seção

---

**Dica**: Use chaves descritivas e organize por contexto para facilitar manutenção!
